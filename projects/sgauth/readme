Для комипиляции программы запустите скрипт ./build

Программа запускается со следующими параметрами:
    > sgauth <server> <port> <login> <password>
или
    > sgauth <config_file_name>
или
    > sgauth

В последнем случае используется конфигурационный файл /etc/sgauth.conf

При подключении браузером по адресу http://localhost:5580
можно посмотреть статистику.
